apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ingress-virtual-hosts
  namespace: default
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  rules:   
  - host: timeserver.master.kubernetes.volnenko.ru
    http:
      paths:
      - path: /    
        pathType: Prefix    
        backend:
          service:
            name: timeserver-server-service
            port:
              number: 80
